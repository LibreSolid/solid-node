## 1. Establish red-first producer parity

- [ ] 1.1 Add `ExportBuildSnapshotParityTest::test_recreated_rebound_child_names_match` in `tests/test_export.py` with independent export/build instances of an assembly whose `render()` recreates and rebinds a child; exercise `export_node` and `Builder._write_viewer_snapshot`, then capture the required base failure `manifest.json == ['Cube']` versus `viewer.json == ['gear']` from `PYTHONPATH="$PWD" ../../../.venv/bin/python -m pytest tests/test_export.py::ExportBuildSnapshotParityTest::test_recreated_rebound_child_names_match -q`.
- [ ] 1.2 Extend the parity fixture and `BuilderLifecycleTest::test_complete_build_writes_viewer_snapshot_before_publishing` with initially failing assertions for common format, version, animation and node fields including `mtime`, while separately asserting portable export versus build-root-relative model paths and the absence of an export-style `models/` tree or copied mesh from a build publication.
- [ ] 1.3 Add focused boundaries for explicit names, plain/list/tuple attribute names, exact `children-<index>` derivation when a child is reachable only through `self.children`, class-name fallback when no parent attribute references a child, rigid short-circuiting, raw operations, colour, and the existing non-list non-rigid path; assert exact strings and record which assertions are red before implementation.

## 2. Introduce the shared serializer

- [ ] 2.1 Add the framework-owned serializer under `solid_node/core/` with one recursive walk that emits the common node fields, applies `_link_child` before recursion, stops at rigid nodes, and obtains each rigid model path from a producer-supplied mapper.
- [ ] 2.2 Replace `solid_node/core/export.py::_serialize_tree` with the shared serializer and an export mapper that preserves `models/` rooting, STL copying, and deduplication; add per-node `mtime` without changing `manifest.json`, `format`, `version`, or animation behavior.
- [ ] 2.3 Replace `solid_node/core/builder.py::_viewer_state` with the shared serializer and a build mapper that preserves build-root-relative paths and introduces no model copying; add `format: "solid-node-export"` while retaining `viewer.json`, version, animation, and atomic publication.
- [ ] 2.4 Remove only the two retired producer-local serializer definitions; leave live `NodeAPI`, `SnapshotNodeAPI`, and development viewer code intact for F4.

## 3. Prove V1 and V2 behavior

- [ ] 3.1 Run `PYTHONPATH="$PWD" ../../../.venv/bin/python -m pytest tests/test_export.py::ExportBuildSnapshotParityTest tests/test_node_naming.py -q` and require identical linked names, fields, tree shape, fallback behavior, and raw operations with distinct correct model roots.
- [ ] 3.2 Run `PYTHONPATH="$PWD" ../../../.venv/bin/python -m pytest tests/test_export.py tests/test_builder_lifecycle.py tests/test_sphinx_ext.py tests/test_web_viewer.py::StlEndpointTest::test_snapshot_node_api_serves_without_loading_project tests/test_build_publication.py -q` to prove both exact documents, Sphinx format recognition, snapshot serving, non-portable build boundaries, and atomic publication.
- [ ] 3.3 Retain canonical `manifest.json` and `viewer.json` examples from the parity fixture under `${XDG_CACHE_HOME:-$HOME/.cache}/hermes/verification/sprint-002/<run-id>/` and record them in that run's rolling manifest with commands, paths, and the cycle commit under test, without committing generated artifacts.

## 4. Prove V18 and V19 boundaries

- [ ] 4.1 Run `rg -n '(_serialize_tree|_viewer_state)|_link_child\(child\)' solid_node tests` and require no retired production definition, call site, or stale test dependence; enumerate every allowed match, including F4-owned `NodeAPI`, `SnapshotNodeAPI`, development `node.ts`, and tests, rather than claiming final zero duplication.
- [ ] 4.2 Run `rg -n 'manifest\.json|viewer\.json|solid-widget\.js|data-solid-widget|SolidNodeWidget' solid_node tests openspec/specs docs` and review the canonical names; run `rg -n '"publishConfig"|npm publish|registry\.npmjs\.org' .github Makefile solid_node/viewers/widget/package.json` and require no new registry-publication configuration or compatibility alias; require `git diff --exit-code 6f8a5ae -- .github Makefile solid_node/viewers/widget/index.html solid_node/viewers/widget/build.mjs solid_node/viewers/widget/package.json` to prove the F1 diff leaves publication configuration and canonical host surfaces unchanged.
- [ ] 4.3 Run V19-focused coverage with `PYTHONPATH="$PWD" ../../../.venv/bin/python -m pytest tests/test_export.py tests/test_sphinx_ext.py tests/test_widget_e2e.py -q`, retaining `solid_node/viewers/widget/index.html` as canonical. Record an expected widget-E2E skip when the ignored bundle/playwright prerequisite is absent; F2 and final paired validation own the browser pass.
- [ ] 4.4 Record for final paired validation the shop-sprint command `rg -n '"(three|@types/three|jokenizer)"|STLLoader|composeOperations|MeshNormalMaterial' floor/frontend` and the framework-sprint command `rg -n 'class SnapshotNodeAPI\b|def (_serialize_tree|_viewer_state)\b|STLLoader|composeOperations|MeshNormalMaterial' solid_node/viewers/web solid_node/core`; F1 records its serializer disposition while S2 and F4 own the remaining removals.

## 5. Complete framework evidence

- [ ] 5.1 Run the complete framework suite from the cycle worktree with `PYTHONPATH="$PWD" ../../../.venv/bin/python -m pytest tests/` and record the exact result and content commit for Sprint 002 integration evidence.
- [ ] 5.2 After focused and complete tests confirm the design, create the next framework ADR recording that `solid-node-export` identifies the shared versioned tree-document schema while producer portability differs; reconcile ADR-020 and ADR-031, update the ADR index and `docs/architecture.md`, and record accepted committed-export `mtime` churn.
- [ ] 5.3 Reconcile every implementation checkbox and record the completed test, inventory, architecture, and ADR-disposition evidence needed by the subsequent supported OpenSpec synchronization, archival, and final-validation workflow; do not treat apply readiness, green tests, archival, or sprint membership as integration authority.
- [ ] 5.4 Record that S2 owns the additive `viewer.json` format update in `shop-skills/solid-node-api/SKILL.md` and the one ordinary first-rebuild watcher hash change; do not edit shop-owned files in F1.
- [ ] 5.5 Run `openspec validate unified-node-serializer --strict`, synchronize accepted `export` and `build-viewer-artifacts` requirements into baseline specs, archive the change, run strict validation again, and include implementation, ADR, architecture, tests, synchronized specs, archive, and exact evidence in the single implementation commit 2.
