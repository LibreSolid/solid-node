## 1. Ratified planning boundary

- [ ] 1.1 Obtain explicit pilot ratification of proposal, design and complete test-framework delta; validate, commit planning and wart evidence only, and verify a clean branch exactly one commit above the recorded base.

## 2. Red-first assertion boundary

- [ ] 2.1 Add representation-tagged signed-result tests for assembly interference; run the finite-negative faceted cases red before implementation, including the Voron measurement and a negative-then-positive sequence.
- [ ] 2.2 Pin empty/±zero, smallest and ordinary positive, non-finite and exact-negative behavior, plus unchanged strict pairwise/fit use of raw non-empty faceted results.
- [ ] 2.3 Change only the assembly assertion's finite-negative faceted interpretation; run the focused suite green without a volume epsilon, shared-helper normalization or input changes.

## 3. Regression and originating project proof

- [ ] 3.1 Run assembly-integrity, exact/faceted policy, shared-verdict/cache and broad-phase unit regressions, plus the CLI strict-contact and assembly-integrity meta tests; record commands, counts and any environment limitations.
- [ ] 3.2 Run the unchanged Voron SourceCorner through this bench on both kernels: require the assembly assertion to pass both, exact pairwise to pass and faceted strict pairwise to remain red. Record the distinction rather than claiming the complete diagnostic suite is green.
- [ ] 3.3 Mutate the fixed sign decision back to its old behavior and prove the negative regression red; mutate away positive-volume rejection and prove the smallest-positive guard red; restore and rerun green.
- [ ] 3.4 Inspect whether implementation introduced an architectural decision; record ADR disposition, clarify the architecture synthesis where warranted, and update the wart with measured resolution without claiming the printer is complete.

## 4. Framework closeout and handoff

- [ ] 4.1 Validate, sync the complete accepted delta into baseline specs, verify no existing scenario was lost, archive the completed change and run final relevant regression.
- [ ] 4.2 Verify repository root and staged paths; commit the full implementation and completed record as commit 2, and verify a clean worktree exactly two commits above the base.

Integration remains a separate authority gate after cycle completion. On
explicit direction, recheck clean unchanged main, fast-forward, verify both
commits and safely tear down this bench. Then return to the Voron project's
own active change, correcting its diagnostic wording/test intent and resuming
the measured source-overlap inventory and moving printer. Project edits and
commits never belong to this framework cycle.
