## 1. Reproduction and compatibility baseline

- [x] 1.1 Record base, worktree, dependency versions and relevant project/viewer states in cycle evidence; preserve existing numeric fixtures and manageable legacy expression outputs before implementation.
- [x] 1.2 Add resource-bounded doubling and 10,000-term deep-chain regressions through actual time/driver construction and build/export paths; prove the relevant failures red without exhausting the host.
- [x] 1.3 Add a small carry-to-piecewise-to-flexible-parameters reproducer with a shared rigid operation; record its red result and source connection to Curta.
- [x] 1.4 Pin legacy combinations in both operand orders, reflected operators, comparisons, abs, all symbolic builtins, scad_inline, bare-name spelling, truth refusal and standalone operation round-trip.

## 2. Native graph and author-facing compatibility

- [x] 2.1 Implement native immutable literal/input/operator/call nodes that preserve operand references without rendering, recursive hashing or process-global strong ownership.
- [x] 2.2 Implement iterative graph traversal, structural interning and repeated-occurrence analysis over final roots, excluding abandoned temporaries and preserving operand multiplicity.
- [x] 2.3 Add the graph-backed SolidPython facade and explicit legacy-text import boundary; override all supported eager operator paths and verify real reflected dispatch.
- [x] 2.4 Route framework time, qualified drivers and symbolic math through graph values while preserving numeric and declared parameter faces; run the focused algebra, driver and time regressions.

## 3. Document publication and flexible dependencies

- [x] 3.1 Add internal operation/flexible scalar collection that keeps graph roots intact; compile native and legacy inputs together into existing deterministic bindings and string slots.
- [x] 3.2 Implement graph-based reconstruction verification, name collision handling and bounded diagnostics; preserve unknown legacy text and truncated warnings.
- [x] 3.3 Connect builder, exporter, browser snapshot and fixture generation to the common compiler; prove no graph object, private id or SCAD-local closure reaches final JSON.
- [x] 3.4 Replace flexible time string searches with structural free-input inspection; prove time-fed omission, driver-only refusal, shared flexible parameters and restored driver state.

## 4. SCAD boundary and round-trip

- [x] 4.1 Emit self-contained shared scalar let closures with collision-free local names; retain bare and unshared spelling and eliminate eager scalar text on normal paths.
- [x] 4.2 Add the restricted closure importer, including dependency order, nested scope and shadowing; recover supported legacy function wrappers without emitting let syntax to viewer documents.
- [x] 4.3 Update standalone operation serialization/unserialize and audit Solid2Node.as_number; prove semantic round-trip, numeric placement, keyframe changes and unchanged live values after publication.
- [x] 4.4 Evaluate generated closures through installed OpenSCAD on a batched boundary-value fixture; compare recorded numeric expectations with an explicit tolerance and test large shared-output growth separately.

## 5. Cross-runtime and resource proof

- [x] 5.1 Run existing math, declarative algebra, ports, couplings, joints, time, flexible-document, expression-binding and producer tests; retain independent expected values when expression spelling changes.
- [x] 5.2 Check the old parity corpus and newly published expressions against the existing viewer evaluator, including all builtins, qualified drivers, piecewise knots and binding dependencies; record the viewer content tested without editing it.
- [x] 5.3 Measure repeated reuse, deep graphs and multiple roots through construction, document and SCAD output; verify linear construction growth, bounded diagnostics and reclaimed discarded graphs across repeated builds.
- [x] 5.4 Document the compound-text migration and exact compatibility limits, preserving the recorded remainder-sign caveat and all modelling backends.

## 6. Curta acceptance

- [x] 6.1 Verify the independent Curta repository root, preserve its state, choose separate evidence outputs and establish an aggregate 8,000,000,000-byte memory limiter before running the full machine.
- [x] 6.2 Export complete Curta using this worktree explicitly with existing geometry caches; record enforcement, process-tree peak, elapsed time, document size, graph/binding counts and exact content identities.
- [x] 6.3 Repeat with fresh geometry in a separate build location, preserving existing caches; record cold-build evidence and any independent geometry cost without weakening acceptance.
- [x] 6.4 Load the export in the existing browser viewer, exercise representative driver states and carry transitions, inspect spring deformation, and record screenshots plus numerical pose/parameter checks.
- [x] 6.5 Report actual warm/cold measured memory peaks in bytes and MiB/GiB, method and limitations, cap headroom and comparison with the prior failure; include these numbers in the durable evidence and final handoff, without claiming the cap or observed peak is a minimum hardware requirement.

## 7. Complete the repository record

- [x] 7.1 After implementation confirms the design, extract the accepted ADR, explicitly supersede the affected ADR-080 decisions, and update the ADR index and architecture synthesis without rewriting historical measurements.
- [x] 7.2 Run supported OpenSpec spec synchronization and archival; include the finished tasks, compatibility documentation and caller evidence in the completed record.
- [x] 7.3 Run final relevant checks and OpenSpec validation, verify repository identity, and make the single completed implementation commit after the planning commit; report both commits and leave integration pending pilot direction.
