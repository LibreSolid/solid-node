## 1. Establish red regressions and old geometry evidence

- [x] 1.1 After pilot ratification, validate and commit the complete planning state; verify the clean bench is exactly one planning-only commit above the recorded base before implementing.
- [x] 1.2 Capture red tests for imported-mesh fusion with OpenSCAD blocked and native geometry/export with SCAD presentation disabled; keep a real Solid2/OpenSCAD leaf as a requiring-path positive control.
- [x] 1.3 Capture old fusion artifacts and predeclare fixture-specific comparison tolerances for overlapping, contained, identical, disjoint, face-touching, edge-touching, near-coincident, transformed, nested and curved mixed-backend cases; record tool versions and expected invalid-input failures.
- [x] 1.4 Add red lifecycle/adapter tests for native participation without an as_scad override, legacy overrides on built-in subclasses, warm CAD-render skips, source aggregation, whole-tree simulation ordering, repeated bindings and empty composition.

## 2. Separate preparation, native producers and SCAD presentation

- [x] 2.1 Extract the native preparation path on the existing node tree, preserving child naming, validation, source scopes, motion phase ordering and local-frame geometry; separate preparation and presentation memo state.
- [x] 2.2 Move exact and sheet artifact production out of SCAD conversion while retaining BREP-before-STL ordering, exact fusion, declared precision, DXF requirements and warm-cache behavior.
- [x] 2.3 Move STL and JSCAD artifact production out of SCAD conversion while preserving imported-mesh admission/adjustment and external-tool behavior; exercise an actual JSCAD subprocess fixture.
- [x] 2.4 Keep Solid2/OpenSCAD native source production and subprocess rendering at their backend boundary; implement and test explicit legacy SCAD-only override dispatch without exception-driven fallback.
- [x] 2.5 Carry flexible specifications and bindings through native preparation without SCAD snapshot production; preserve numeric mesh/exact evaluation, wiring failures and structural identity.
- [x] 2.6 Implement the SCAD compatibility consumer preserving assemble/as_scad/scad_code/generate_scad behavior, compact graph closures, colours, transform order, optimized imports and flexible snapshot limitations.

## 3. Produce faceted fusions directly and migrate affected caches

- [x] 3.1 Add red recipe-currency tests for old fusion caches, missing/changed recipes, content-restamp bypass, nested dependencies, unchanged exact artifacts and repeated shared instances.
- [x] 3.2 Introduce narrowly scoped recipe qualification in currency and publication, propagating child recipe changes into enclosing fusion artifacts without changing node identity or filenames.
- [x] 3.3 Implement dependency-ordered faceted fusion through the point-of-use manifold3d resolver with local-frame child transforms, engine input/result checks, atomic STL publication and bounded temporary ownership.
- [x] 3.4 Prove missing-engine, invalid-input, failed-replacement and no-fallback behavior; prove exact fusion and mere assembly grouping do not resolve the mesh engine.
- [x] 3.5 Compare every captured fusion case against analytic expectations and old artifacts using bounds, volume, symmetric difference, surface distances and topology; return unexplained compatibility differences to the pilot before continuing.

## 4. Route consumers and preserve publication safety

- [x] 4.1 Route export, test-runner geometry builds, web snapshot staging and browser development through native preparation and materialization, carrying output selection across fresh builder processes without changing the viewer protocol.
- [x] 4.2 Preserve ordinary solid build SCAD deliverables and OpenSCAD develop/snapshot selection; ensure SCAD faceted-fusion presentation references the canonical fused artifact rather than rerunning a different union.
- [x] 4.3 Test source changes before/during production and SCAD output, lock ownership, failed subprocesses, partial atomic publication, manifest-last currency, flexible snapshot sweep and repeated SCAD last-occurrence write suppression.
- [x] 4.4 Verify geometry-only runs neither construct assembly SCAD nor produce flexible SCAD snapshots, all native adapters bypass SCAD conversion, and real OpenSCAD invocations are confined to the documented backend/evaluation/viewer boundaries.

## 5. Validate unchanged callers and measure actual cost

- [x] 5.1 Run the full framework suite, adapter/dependency matrix and expression parity tests; explicitly exercise optional web snapshot E2E and real OpenSCAD output, reporting unavailable prerequisites rather than counting mocked coverage as integration proof.
- [x] 5.2 Export unchanged Curta Type I-3x with warm and fresh geometry caches under the measured safety cap; record exact content identities, recipe/cache state, elapsed time, descendant-inclusive peak memory, swap, engine invocation counts and artifact/output sizes.
- [x] 5.3 Run the existing independent numeric-versus-viewer Curta comparison and inspect browser whole-machine/carry-spring poses; compare applicable rigid geometry to the base and verify representative mixed geometry in OpenSCAD too.
- [x] 5.4 Record reproduction commands, raw measurements, comparisons, limitations and any approved compatibility decisions; report actual warm/cold memory next to the prior cycle without treating the cap as a hardware requirement.

## 6. Complete the confirmed framework cycle

- [x] 6.1 Document the native adapter seam, SCAD compatibility bridge, changed fusion dependency/geometry identity and cache migration; extract only confirmed ADR decisions and update the architecture synthesis and ADR index.
- [x] 6.2 Synchronize the ratified delta specs and archive through the supported OpenSpec workflows after all implementation and evidence tasks are complete.
- [x] 6.3 Run final relevant tests and strict OpenSpec validation, verify the framework repository root, commit the complete implementation record as commit 2, and report a clean branch exactly two commits above the base; do not integrate or push without separate authority.
