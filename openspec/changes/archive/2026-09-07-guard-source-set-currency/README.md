# guard-source-set-currency

Guard aggregate artifact timestamps with the recorded metadata state of every
tracked source, using node-scoped content verification whenever that state
changes or is absent.
