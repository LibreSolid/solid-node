# Retired capabilities

These three capabilities left this repository with the viewer. Each file
records every requirement as REMOVED, with the same reason and migration:
the behaviour is unchanged and is now specified by solid-node-viewer. They
are kept beside the change's deltas rather than under `specs/` because the
archive tool will not write a capability with no requirements left, which
is exactly what a wholly relocated capability is.
